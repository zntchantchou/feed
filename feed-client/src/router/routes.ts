export enum AppRoutesEnum {
  login = "login",
  signup = "signup",
  feed = "feed",
  bookmarks = "bookmarks",
  tags = "tags",
  contacts = "contacts",
  search = "search",
  recommended = "recommended",
  mostUpvoted = "most-upvoted",
}

export const RouteNames = {
  login: AppRoutesEnum.login,
  signup: AppRoutesEnum.signup,
  feed: AppRoutesEnum.feed,
  bookmarks: AppRoutesEnum.bookmarks,
  tags: AppRoutesEnum.tags,
  contacts: AppRoutesEnum.contacts,
  search: AppRoutesEnum.search,
  recommended: AppRoutesEnum.recommended,
  mostUpvoted: AppRoutesEnum.mostUpvoted,
};
